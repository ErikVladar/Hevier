{{-- <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    @foreach ($images as $image)
        <img src="{{ $image }}" class="rounded-lg object-cover w-full h-auto" alt="">
    @endforeach
</div> --}}
